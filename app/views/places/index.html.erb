<h1>My Travel Log</h1>
<p>Here are all the places I've visited:</p>
<ul>
  <% @places.each do |place| %>
    <li><%= link_to place.name, place_path(place) %></li>
  <% end %>
</ul>
<%= link_to "Add a New Place", new_place_path %>